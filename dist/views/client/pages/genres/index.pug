extends ../../layouts/default.pug
include ../../mixins/box-head.pug

block main
    .container.my-3
        .row
            .col-12
                +box-head(pageTitle)

        .row
            each genre in genres
                .col-6.mb-3
                    div(class="card")
                        img(src=genre.avatar)
                        div(class="card-body")
                            h5(class="card-title") #{genre.title}
                            p(class="card-text") #{genre.description}
                            a(href=`/songs/${genre.slug}` class="btn btn-primary") Xem chi tiết